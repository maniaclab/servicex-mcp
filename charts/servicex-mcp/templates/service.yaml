apiVersion: v1
kind: Service
metadata:
  name: {{ include "servicex-mcp.fullname" . }}
  labels:
    {{- include "servicex-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "servicex-mcp.selectorLabels" . | nindent 4 }}
